# Rust
/target/

# Node
node_modules/
web/dist/
web/.vite/
npm-debug.log*

# Build artifacts — regenerated by scripts/build-wasm.sh and scripts/build-worklet.sh
web/public/engine.wasm
web/public/worklet/processor.js

# macOS
.DS_Store

# JetBrains
.idea/

# Planning and review documents. Kept out of the repository on purpose: they
# are written in another language than the code, they record intent and
# judgement rather than what was built, and a reader of a public repository is
# owed neither.
PLAN.md
SAMPLER.md
WEB-REVIEW.md
