[package]
name = "protocol"
version = "0.0.0"
edition.workspace = true
repository.workspace = true
# Apache-2.0, not the workspace default: a plugin author must be able to
# implement the ABI without reading AGPL code (ADR-0016).
license = "Apache-2.0"
publish = false

[dependencies]

[dev-dependencies]
serde_json = "1"
