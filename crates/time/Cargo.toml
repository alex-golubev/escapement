[package]
name = "escapement-time"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
publish.workspace = true

# None, and that is the point: this type is named by a crate that runs on the
# audio thread, so anything arriving here arrives there too.
[dependencies]

[lints]
workspace = true
